# Copy to terraform.tfvars and adjust if needed — no AWS secrets go in this
# file. Credentials come from `aws configure` or env vars (see main.tf).

aws_region           = "eu-west-1"
instance_type        = "t3.micro"
ssh_public_key_path  = "~/.ssh/id_rsa.pub"
